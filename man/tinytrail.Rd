% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinytrail.R
\name{tinytrail}
\alias{tinytrail}
\title{Register a script in the project trail}
\usage{
tinytrail(
  description,
  data_source = NULL,
  pin_to_top = FALSE,
  record_runtime = TRUE,
  name = NULL,
  auto = TRUE,
  extra_hooks = NULL
)
}
\arguments{
\item{description}{Character. Description of what the script does.}

\item{data_source}{Character. Optional. Enter the sources of data used in
this script — name the dataset or survey, not a file path
(e.g. \code{"Current Population Survey (BLS)"}).}

\item{pin_to_top}{Logical. Pin this script to the top of the trail. Useful
for a \code{main.R} that sources other scripts — keeps it visible at the top of
\verb{_tinytrail.yaml} regardless of alphabetical order. Default \code{FALSE}.}

\item{record_runtime}{Logical. Record elapsed time on exit. Default \code{TRUE}.}

\item{name}{Character. Override the auto-detected script name. Intended for
use in tests and non-standard execution environments where auto-detection
is unavailable.}

\item{auto}{Logical. Automatically intercept common write functions and
record their output paths. Default \code{TRUE}. Set to \code{FALSE} to use explicit
\code{tinytrail_write()} calls instead.}

\item{extra_hooks}{A \code{list} with elements \code{fn} and \code{arg} specifying
additional write functions to intercept when \code{auto = TRUE}. \code{fn} is a
character vector of function names (\code{"my_save"} for a script-level
function, or \code{"pkg::fn"} for a package function). \code{arg} is a character
vector of the corresponding file-path argument names. Functions from
packages that are not installed are silently skipped.}
}
\value{
\code{name} (the script name), invisibly. Called for its side effect of
creating or updating the YAML trail file in the project root.
}
\description{
Call once near the top of every script. Creates or updates an entry in
\verb{_tinytrail.yaml} and sets the script name so that outputs can be recorded.
}
\details{
By default (\code{auto = TRUE}) common write functions (\code{write.csv}, \code{saveRDS},
\code{readr::write_csv}, \code{ggplot2::ggsave}, etc.) are hooked automatically so
their output file paths are captured without any \code{tinytrail_write()} wrapper.
Set \code{auto = FALSE} to use explicit \code{tinytrail_write()} calls instead.
}
\examples{
\donttest{
withr::with_tempdir({
  writeLines("Package: testproject\nVersion: 0.1.0", "DESCRIPTION")

  tinytrail(
    description    = "Clean and summarise survey data",
    data_source    = "Current Population Survey (BLS)",
    record_runtime = FALSE,
    name           = "clean.R"
  )
  write.csv(mtcars, "clean.csv")
  png("age_dist.png"); hist(mtcars$mpg, main = "MPG"); dev.off()
})
}
}
\seealso{
\code{\link[=tinytrail_write]{tinytrail_write()}} to record output paths explicitly,
\code{\link[=tinytrail_dict]{tinytrail_dict()}} to capture a data dictionary.
}
